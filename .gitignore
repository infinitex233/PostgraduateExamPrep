# Local textbook PDFs are kept on the machine, not in Git.
DigitalBooks/**/*.pdf

# Cache files are derived from PDFs — rebuild locally.
DigitalBooks/Cache/

# Claude Code internal files
.claude/

# Common local clutter
Thumbs.db
.DS_Store
