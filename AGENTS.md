# AGENTS.md

## Scope and authority

These instructions apply to the entire
`D:\Documents\PostgraduateExamPrep` repository. Files in the workspace are the
source of truth. Do not rely on memory from earlier conversations when the
repository can answer the question.

This repository supports preparation for the 11408 postgraduate entrance exam:

- Politics
- English I
- Mathematics I: calculus, linear algebra, probability, and statistics
- 408 Computer Science: data structures, computer organization, operating
  systems, and computer networks

Do not edit this file unless the user asks to change the repository workflow.

## Startup checklist

Before changing files:

1. Read `AGENTS.md`.
2. Read the root `README.md` for the repository overview.
3. For study progress work, read `StudyProgress/README.md` and inspect the
   target records.
4. For textbook lookup, book notes, OCR caches, or material organization, read
   `StudyMaterials/README.md` and inspect the relevant source files.
5. Check `git status --short` before editing. Preserve unrelated worktree
   changes and work with overlapping changes.
6. Preserve user-authored notes, logs, templates, plans, and source materials.
7. If evidence is incomplete, state the gap instead of inventing details.

## Repository map

- `StudyProgress/` contains daily logs, progress indexes, plans, reviews, and
  the generated dashboard.
- `StudyMaterials/408/` contains local 408 textbook PDFs.
- `StudyMaterials/Math/Basic/` contains foundation-stage mathematics PDFs.
- `StudyMaterials/Math/Intensive/` contains intensive-stage mathematics PDFs.
- `StudyMaterials/Cache/408/` contains page-level caches for 408 books.
- `StudyMaterials/Cache/Math/Basic/` contains mathematics foundation caches.
- `StudyMaterials/Cache/Math/Intensive/` contains mathematics intensive caches.
- `StudyMaterials/BookNotes/` contains textbook notes updated chapter by
  chapter.
- `StudyMaterials/English/` contains English source materials and review
  artifacts.
- `scripts/` contains cache, lookup, dashboard, test, and maintenance tools.

## Documentation policy

Every repository README has an English and a Simplified Chinese version:

- `README.md` is the English document and the default page shown by GitHub.
- `README.zh-CN.md` is the Simplified Chinese document.

Each README must link to its counterpart at the top. Keep each language pair
structurally aligned and update both files in the same change. Links must be
relative so they work on GitHub and in local clones.

Agent instruction files such as `AGENTS.md` and `CLAUDE.md` remain in English.
Canonical subject names and literal schema values may remain in Chinese when
the code depends on them.

## File and Git safety

- Keep daily progress records under `StudyProgress/`.
- Keep textbook PDFs, textbook-derived notes, OCR caches, and English source
  materials under `StudyMaterials/`.
- Do not place daily logs in `StudyMaterials/`.
- Keep source materials separate from derived notes.
- Do not rename, move, or delete source materials unless the user explicitly
  requests it.
- Do not overwrite notes, logs, plans, or templates unless the task requires
  it. Merge around user edits where possible.
- The worktree may contain changes made outside the current task. Do not revert
  or rewrite them. Stage explicit paths when the worktree is mixed.
- Textbook PDFs under `StudyMaterials/` are local-only files. Never stage or
  commit them.
- OCR caches under `StudyMaterials/Cache/` are derived data that may be staged
  and committed. Before staging them, verify that the cache files are complete,
  readable, and free of temporary or diagnostic artifacts.
- Do not commit credentials, tokens, cookies, browser profiles, personal export
  files, or machine-specific diagnostics.
- Remove `tmp/`, `__pycache__/`, `.pytest_cache/`, rendered PDF pages, OCR
  diagnostics, PID files, screenshots, and other one-off artifacts after use.
- Do not commit or push unless the user asks. Before either action, inspect the
  staged diff and verify that ignored or sensitive files are absent.

## Study progress workflow

Treat a natural-language daily study report as a logging request unless the
user clearly says they only want to discuss it.

For each report:

1. Create or update
   `StudyProgress/DailyLogs/YYYY-MM/YYYY-MM-DD.md`.
2. Start the file with YAML frontmatter matching
   `StudyProgress/DailyLogs/_template.md`.
3. Preserve the user's original report.
4. Record a concise overview, subject progress, stated time or quantity,
   current state, problems, and next actions when the report supports them.
5. Use integer minutes and canonical subject names. Use `null` when the user
   did not provide a value.
6. Do not invent time, task counts, chapter status, or completion.
7. Update `StudyProgress/ProgressIndex.md` with a short row for the day.
8. Run `python scripts/build_dashboard.py` to regenerate
   `StudyProgress/dashboard.html`.

Use `StudyProgress/Reviews/Weekly/` for weekly reviews and
`StudyProgress/Reviews/Stage/` for stage reviews. Experience-post material is
optional and should be derived later from accumulated records.

## Dashboard maintenance

`StudyProgress/dashboard.html` is generated output. Do not hand-edit it as the
only source of truth, and do not keep parallel dashboard variants.

The compatibility entry point is:

```bash
python scripts/build_dashboard.py
```

Structured daily-log metrics come only from YAML frontmatter; never infer
minutes or progress from prose. Archive and monthly presentation also reads the
stable sections in `StudyProgress/ProgressIndex.md` and optional summaries under
`StudyProgress/DailyLogs/Monthly/`.

Data aggregation lives in `scripts/build_dashboard.py`. Archive enrichment,
rendering, layout, Capsule colors, typography, and stale-output cleanup live in
`scripts/build_dashboard_variants.py`.

The current dashboard is a 1920 x 1080 Capsule-style landscape deck with a warm
paper palette, dark readable text, restrained controls, and a fixed page
sequence. Keep layout, frontmatter, and interaction behavior stable unless the
user asks for a structural change.

Canonical subject names are data keys and must not be renamed:

- `数学-高数`
- `数学-线代`
- `数学-概率`
- `专业课-数据结构`
- `专业课-组成原理`
- `专业课-操作系统`
- `专业课-计算机网络`
- `英语`
- `政治`

Use `subject_colors` for concrete subjects and `group_colors` for the aggregate
groups `数学`, `专业课`, `英语`, `政治`, and `其他`. A concrete subject must use the
same color in charts, legends, totals, and progress indicators.

After dashboard code or visual changes, run:

```bash
python -m unittest scripts.test_build_dashboard scripts.test_build_dashboard_variants
python scripts/build_dashboard.py
```

Inspect the generated HTML when CSS or layout changes.

## Textbook lookup workflow

Search the local cache before opening a large PDF:

```bash
python scripts/query.py "keyword"
python scripts/query.py "keyword" --book "book name"
python scripts/query.py "keyword" --book "book name" --page-only
python scripts/query.py "keyword" --book "book name" --context 2
```

`scripts/query.py` recursively reads
`StudyMaterials/Cache/**/*.docling.json`. The page-level format is:

```json
{
  "book": "book name",
  "total_pages": 100,
  "pages": [
    {"page_no": 1, "text": "..."}
  ]
}
```

Cache hits identify candidate PDF pages. Inspect the PDF when exact wording,
formulas, diagrams, examples, or printed page numbers matter. OCR text may be
wrong.

When answering a page-location question, provide both:

- `书内印刷页码` for the printed page number
- `PDF 页码` for the PDF page number

If either value cannot be confirmed, say so explicitly. Do not fabricate
textbook wording, page numbers, examples, formulas, or conclusions.

Use `scripts/page_ocr.py` as the primary builder for page-level caches. It
discovers PDFs recursively and writes caches to the matching source-relative
category using `scripts/cache_layout.py`. `scripts/docling_cache.py` is a
legacy-compatible alternative; `scripts/query.py` reads both cache formats
recursively and prefers categorized copies over legacy flat duplicates.

## Book note workflow

Create one Markdown note per textbook under `StudyMaterials/BookNotes/`. Update
notes chapter by chapter during review instead of producing a full-book summary
in one pass.

When updating a chapter:

- Read the corresponding source pages.
- Merge verified material into the existing chapter.
- Preserve the user's additions, deletions, ordering, and annotations.
- Separate textbook content from the agent's explanations or supplements.
- Add page references for definitions, formulas, theorems, examples, key
  conclusions, and common mistakes when possible.
- Prefer frameworks, definitions, exam patterns, error traps, and useful entry
  points over reproducing the book.

## English materials workflow

Keep English materials and generated review artifacts under
`StudyMaterials/English/`. Current writing-template artifacts live at:

```text
StudyMaterials/English/WritingTemplates/
  index.html
  index.pdf
```

When extracting text from screenshots or PDFs, preserve source order and omit
watermarks, platform chrome, and screenshot-only noise. If the user requests a
PDF version after `index.html` changes, regenerate `index.pdf` so the two files
stay aligned.

## Verification and handoff

- Run checks that match the risk of the change.
- Confirm generated files are readable and organized.
- Verify README language links and referenced paths after documentation edits.
- After Python scripts or tests run, remove generated Python cache directories.
- Report checks that could not be run.
- List the exact changed paths when handing work back.
